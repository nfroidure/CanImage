CanImage : Image editor for Firefox

CanImage ist a Firefox add-on enablign you to modify images from many source inside Firefox. Once you did, you can push her everywhere you want (input[type=file], input[type=text]|textarea as dataUri, clipboard as Bitmap or DataUri, any hardrive).
